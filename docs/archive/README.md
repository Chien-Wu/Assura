# Historical designs and test results

These documents preserve previous implementation decisions, provider versions and test observations. Use the [current documentation index](../README.md) for setup.

- `participant-rag-*`, `voice-integration.md` and `elevenlabs-agent.md`: the earlier six-tool interview/RAG flow. The current ordinary recorder has two tools and does not run that interview protocol.
- `elevenlabs-workflow-integration.md` and `workflow-backend-results.md`: native specialist routing with the earlier webhook bridge. The current separate app test uses client tools.
- `workflow-lab-results.md`: the initial transfer and context experiments.
- `ai2-stage-contract.md`: the retirement notice for conversational AI2. Historical evidence remains readable.

Archived prose may describe configuration that has since changed. Current config links point to maintained files; recover a matching historical artifact from the relevant Git revision before attempting a historical reproduction. Do not apply archived configuration to the shared current agent.
